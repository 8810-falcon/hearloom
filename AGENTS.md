# コミュニケーションルール
AIエージェントとユーザーは、必ず日本語でやり取りを行うようにしてください。

---

# 🚨 最重要ルール：エージェントエラー時の対応フロー

専門エージェント起動時にエラーが発生した場合、以下のフローに**必ず**従うこと：

## 1. エラー原因の分析と提示
エラーが発生したら、まず**ユーザーにエラーの原因を明確に説明**する。

**提示内容:**
- 何が起きたか（エラーメッセージの解説）
- なぜ起きたか（技術的な原因）
- 今後同じエラーを避けるための知見

## 2. 代替案の検討と提示
エラー原因を分析した上で、**複数の代替案をユーザーに提示**する。

**代替案の例:**
- 別のエージェントに依頼
- 同エージェントを新規セッションで起動（resumeしない）
- メインのClaude Codeが直接対応
- 時間を空けて再試行

**提示フォーマット:**
```
## どう進めますか？

### A. [代替案1]
[メリット・デメリット]

### B. [代替案2]
[メリット・デメリット]

### C. その他
ご提案があればお聞かせください。
```

## 3. ユーザー承認を待つ
代替案を提示したら、**必ずユーザーの承認を待つ**こと。

**ユーザーの回答形式:**
- **yes** / **A** / **B** 等の選択
- **no**（代替案が不適切な場合）
- **他のやり方**（ユーザー独自の提案）

**重要**: ユーザーの承認なしに勝手に作業を進めてはならない。

## 4. 承認後に作業再開
ユーザーが承認した方法で、作業を再開する。

---

# 🚨 最重要ルール：徹底的なフィードバック原則

**すべてのエージェントは、ユーザーの企画や案に対して、バチバチに厳しくフィードバックすること。**

## フィードバックの原則

### 1. 需要の妥当性を徹底検証
ユーザーが提案したアイデアや機能に対して、**実際に需要がありそうか**を厳しく評価する。

**問うべき質問:**
- この課題は本当に存在するのか？
- ターゲットユーザーは本当にこれを求めているのか？
- 既存の解決策で十分ではないのか？なぜ新しいアプリが必要なのか？
- 競合との差別化要素は何か？

### 2. 事実ベースの分析
フィードバックは必ず**事実の分析に基づいた発言**とすること。

**NGな発言:**
- 「素晴らしいアイデアですね！」（根拠なき肯定）
- 「きっとユーザーに喜ばれます」（希望的観測）
- 「これは流行りそうです」（感覚的な判断）

**OKな発言:**
- 「類似アプリXは月間10万DLですが、Y機能が欠けている。Hearloomはそこを狙えます」（競合分析）
- 「このターゲット層は、過去の調査データによると〇〇な傾向があります」（データ参照）
- 「この機能は技術的に実装困難です。理由は...」（技術的事実）

### 3. 建設的な批判
厳しくフィードバックしつつも、**建設的な代替案**を提示すること。

**フィードバックフォーマット:**
```
❌ 懸念点: [具体的な問題]
📊 根拠: [事実・データ・分析]
💡 提案: [代替案や改善策]
```

### 4. 妥協しない品質基準
「ユーザーが気に入っているから」という理由で、問題のあるアイデアを通してはならない。

**重要**: いいものを作るために、遠慮なく厳しい意見を言うこと。それがプロフェッショナルの責任。

---

# Hearloom プロジェクト概要

## プロダクトビジョン
※これから企画フェーズで具体化予定
- アプリ名: Hearloom
- 現状: コンセプト検討中
- ターゲット: TBD
- 解決する課題: TBD

## 技術スタック

### アーキテクチャ方針
- **ハイブリッドアーキテクチャ（ネイティブシェル + WebView UI）**
- ネイティブ機能とUI層を分離し、開発効率とUX品質を両立
- 各プラットフォームの強みを活かしつつ、UI実装を共通化

### 責務分離

#### ネイティブシェル（iOS: Swift + SwiftUI / Android: Kotlin + Jetpack Compose）
- WebViewのホスティングとライフサイクル管理
- プラットフォーム固有機能の実装
  - iOS: バックグラウンドフェッチ、Share Extension、Push Notification
  - Android: WorkManager、Share Intent、FCM
- ネイティブ↔WebView間のブリッジAPI提供
- データ永続化（ローカルDB、キャッシュ）

#### Web UI（React + TypeScript）
- アプリ内UIの実装（HTML/CSS/JavaScript）
- 聴取履歴表示、感情タグ編集、時系列ビュー、設定画面など
- ブリッジAPI経由でネイティブ機能を呼び出し
- 状態管理とUIロジック

### プラットフォーム
- **iOS/Android両対応**
- 最小サポートバージョン: TBD（企画時に決定）
- Webデプロイ: 将来的にWeb版も展開可能

### 開発環境

#### iOS（ネイティブシェル）
- 言語: Swift
- IDE: Xcode
- UIフレームワーク: SwiftUI
- WebView: WKWebView
- ブリッジ: WKScriptMessageHandler
- プロジェクト構成: `/ios/` ディレクトリ配下

#### Android（ネイティブシェル）
- 言語: Kotlin
- IDE: Android Studio
- UIフレームワーク: Jetpack Compose
- WebView: Android WebView
- ブリッジ: JavaScriptInterface
- プロジェクト構成: `/android/` ディレクトリ配下

#### Web UI
- 言語: TypeScript
- フレームワーク: React
- ビルドツール: Vite
- スタイリング: CSS Modules / Tailwind CSS（実装時に決定）
- 状態管理: Context API / Zustand（実装時に決定）
- プロジェクト構成: `/web/` ディレクトリ配下

### プロジェクト構造
```
Hearloom/                          # プロジェクトルート
├── .claude/                       # AI関連（エージェント設定）
├── CLAUDE.md                      # AI関連（プロジェクト情報）
├── SKILL.md                       # AI関連（開発知見）
├── docs/                          # ドキュメント
├── ios/                          # iOSネイティブシェル
│   ├── Hearloom.xcodeproj
│   ├── Hearloom/
│   │   ├── App/                  # アプリエントリーポイント
│   │   ├── WebView/              # WebView統合層
│   │   ├── Bridge/               # ネイティブ↔Web通信
│   │   ├── Native/               # ネイティブ機能（API連携等）
│   │   └── Resources/
│   ├── HearloomTests/
│   └── HearloomUITests/
├── android/                       # Androidネイティブシェル
│   ├── app/
│   │   └── src/
│   │       └── main/
│   │           ├── java/com/hearloom/
│   │           │   ├── app/      # アプリエントリーポイント
│   │           │   ├── webview/  # WebView統合層
│   │           │   ├── bridge/   # ネイティブ↔Web通信
│   │           │   └── native/   # ネイティブ機能
│   │           └── res/
│   └── gradle/
└── web/                          # Web UI（React）
    ├── src/
    │   ├── components/           # UIコンポーネント
    │   ├── screens/              # 画面
    │   ├── bridge/               # ブリッジAPI呼び出し
    │   ├── hooks/                # カスタムフック
    │   ├── stores/               # 状態管理
    │   ├── styles/               # スタイル
    │   └── utils/                # ユーティリティ
    ├── public/                   # 静的ファイル
    ├── package.json
    ├── vite.config.ts
    └── tsconfig.json
```

### フレームワーク・ライブラリ

#### iOS
- WebView: WKWebView
- ブリッジ: WKScriptMessageHandler
- データ永続化: TBD（Core Data / Realm / UserDefaults）
- アーキテクチャパターン: MVVM

#### Android
- WebView: Android WebView
- ブリッジ: JavaScriptInterface + @JavascriptInterface
- データ永続化: TBD（Room / DataStore / SharedPreferences）
- アーキテクチャパターン: MVVM

#### Web UI
- フレームワーク: React 18+
- 言語: TypeScript
- ビルド: Vite
- スタイリング: TBD（CSS Modules / Tailwind CSS / Emotion）
- 状態管理: TBD（Context API / Zustand / Redux Toolkit）
- ルーティング: React Router（必要に応じて）

## 開発フェーズ
**現在: 企画フェーズ**
- [ ] プロダクト企画（コンセプト、機能定義、ユーザージャーニー）
- [ ] UI/UXデザイン
- [ ] 実装
- [ ] テスト
- [ ] リリース準備

## AI協働ガイドライン

### 企画フェーズ
- product-planning-partnerエージェントとの対話でコンセプトを固める
- 決定事項はSKILL.mdに随時記録
- 技術選択の判断材料が出たら、mobile-tech-leadにレビュー依頼

### デザインフェーズ
- ui-ux-designerエージェントでデザインシステムを言語化
- **Web UI（React）前提**でコンポーネント設計を行う
- ネイティブとWebViewの**責務分離**を明確化（どこまでネイティブ、どこからWeb UI）
- デザインの実装可能性はweb-ui-developerとmobile-tech-leadの両方にレビュー依頼
- デザインの意図と実装の詳細をSKILL.mdに記録

### 実装フェーズ

#### Web UI実装
- web-ui-developerエージェントが主担当
- Reactコンポーネントの実装、ブリッジAPI呼び出し、状態管理
- ブラウザでの動作確認後、ネイティブアプリに統合
- 新規機能は必ずテストとセットで実装

#### ネイティブシェル実装
- mobile-tech-leadエージェントが主担当
- WebViewホスティング、ブリッジAPI提供、プラットフォーム固有機能
- iOS/Android各プラットフォームのコーディング規約を実装開始時に確定
- プラットフォーム間で共通仕様を保ちつつ、各プラットフォームの強みを活用

#### 統合テスト
- Web UI単体テスト、ネイティブ単体テスト、統合テストの3層でテスト
- ブリッジAPIの動作確認は必須

## 品質基準
※実装フェーズで具体化予定

## 開発フロー
- Git管理: 実施中
- ブランチ戦略: TBD
- コミットメッセージ規約: TBD

## ドキュメント管理ポリシー

### 分割の基準
- 単一ファイルが300行を超えたら分割を検討
- 異なるドメイン（企画/技術/デザイン）の情報は別ファイル
- 頻繁に更新される情報と、確定した情報は分離

### 追記の基準
- 既存カテゴリの情報（50行未満）→ 既存ファイルに追記
- 新トピック（100行以上）→ 新規ファイル作成
- 迷った場合 → SKILL.mdに一時記録し、肥大化したら分割

### ドキュメント作成・更新の責任範囲
- **product-planning-partner**: docs/product/配下のコンテンツ作成・更新
- **ui-ux-designer**: docs/design/配下のコンテンツ作成・更新
- **mobile-tech-lead**: docs/tech/配下のコンテンツ作成・更新
- **mobile-app-foundation-architect**: ドキュメント構造設計、肥大化ドキュメントのリファクタリング、ルール策定

### ワークフロー
1. 各専門エージェントは、作業完了時に該当ドメインのドキュメントを更新
2. SKILL.mdに最新決定事項（過去7日間）を記録
3. ドキュメントが肥大化（300行超）したら、mobile-app-foundation-architectに整理を依頼

---

# エージェント起動ルール

## 基本原則

**透明性の確保**: エージェント起動時は担当エージェント名と作業内容を宣言。完了時は成果物と次のステップを報告。

## エージェント選択のポイント

- **product-planning-partner**: コンセプトのブレインストーミング、MVP定義、機能優先順位付け
- **ui-ux-designer**: デザインシステム策定、画面設計、アクセシビリティ検討、ネイティブ/Web UI責務分離
- **web-ui-developer**: Web UI（React）実装、ブリッジAPI統合、Webパフォーマンス最適化
- **mobile-tech-lead**: ネイティブシェル実装、ブリッジAPI設計、プラットフォーム固有機能、複雑な統合
- **mobile-app-foundation-architect**: プロジェクト基盤改善、開発ワークフロー最適化、プロセス課題解決
- **claude-code-guide**: Claude Code公式ベストプラクティスの照会、CLAUDE.md/エージェント設定の最適化相談
- **メインのClaude Code**: ファイル操作、単純な修正、Git操作、情報整理

**判断が難しい場合**: 専門エージェントを優先。時間的制約があればメインで対応。

## Claude Code最適化時のルール

### 原則
Claude Code自体の機能・設定・使い方に関する改善を行う際は、**必ずclaude-code-guideエージェントに公式のベストプラクティスを確認**すること。

### 対象となる作業
以下の作業を行う際は、claude-code-guideとの連携が必須：

- **SKILL.mdの構造設計**: 分割戦略、命名規則、参照方法の最適化
- **エージェント設定の最適化**: config.jsonの構造、モデル選択基準、パラメータ調整
- **ワークフロー設計**: Claude Codeの機能を活用した開発フローの構築
- **プロジェクト構成**: Claude Codeが効率的に動作するディレクトリ構造の設計
- **ツール統合**: Claude Codeと他ツール（Git、IDE等）の連携方法

### 連携フロー
1. **事前確認**: mobile-app-foundation-architectが改善案を起案
2. **公式照合**: claude-code-guideに公式ベストプラクティスを確認依頼
3. **統合判断**: 公式推奨とモバイルアプリ特有の要件を統合
4. **実装**: 最終案をユーザーに提示し、承認後に実装

### claude-code-guideの使用例

**問い合わせ方法:**
```
claude-code-guideエージェントを起動し、以下を確認してください：
「CLAUDE.mdが300行を超えています。公式の推奨構造と、モバイルアプリプロジェクトに適した分割方法を教えてください」
```

**想定される質問例:**
- 「CLAUDE.mdとPROJECT.mdの使い分けは？」
- 「コンテキストウィンドウ管理のベストプラクティスは？」
- 「サブエージェントを使うべきタイミングは？」
- 「Plan Modeの効果的な使い方は？」
- 「permissionsとsandboxの推奨設定は？」

### 例外
以下の場合はclaude-code-guideへの確認は不要：

- モバイルアプリ固有のドメイン知識の記録（SKILL.mdへの開発知見追加等）
- 既に確立されたパターンの単純適用
- Claude Codeの機能に直接関係しない、プロジェクト固有の設定